namespace SmartScheduler.Domain.Enums;

/// <summary>
/// Enum representing user roles in the SmartScheduler system.
/// </summary>
public enum UserRole
{
    Dispatcher = 0,
    Customer = 1,
    Contractor = 2
}

